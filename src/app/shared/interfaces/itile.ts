export interface ITile {
  word?: string;
  url?: string;
  svg?: string;
  coordinates?: object[];
  letter?: string;
  direction?: string;
  isWord?: boolean;
  isFound?: boolean;
  // isSelected?: boolean;
}
